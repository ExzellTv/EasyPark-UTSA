# app/utils/parking_map.py

# For now, a simple placeholder list of spot polygons
PARKING_SPOTS = [
    # each tuple might represent (x1,y1,x2,y2) bounding boxes
    ((100, 200), (150, 250)),  
    ((200, 200), (250, 250)),  
]
    # Add more spots as needed